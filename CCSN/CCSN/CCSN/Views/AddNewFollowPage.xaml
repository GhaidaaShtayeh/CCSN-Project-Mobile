<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CCSN.Views.AddNewFollowPage" BackgroundColor="AliceBlue"
             xmlns:viewmodels="clr-namespace:CCSN.ViewModels" xmlns:xct="http://xamarin.com/schemas/2020/toolkit">
    <ContentPage.BindingContext>
        <viewmodels:AddNewFollowModelView/>
    </ContentPage.BindingContext>
    <ContentPage.Behaviors>
        <xct:EventToCommandBehavior Command="{Binding Appearing}" EventName="Appearing" />
    </ContentPage.Behaviors>
    <NavigationPage.TitleView>
        <StackLayout Orientation="Horizontal">
            <Label  Style="{StaticResource navlabel}"
                Text="add new follow-up"/>
        </StackLayout>
    </NavigationPage.TitleView>
    <ScrollView>
    <StackLayout>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
            </Grid>
            <StackLayout Orientation="Horizontal"  Grid.Row="0" >
                <Label HorizontalOptions="Start" FontSize="Large" VerticalOptions="Center" Text=" - Follow Up Date :" ></Label>
                <Frame
    CornerRadius="10"       
	Padding="5"
      Margin="4"          
	HasShadow="True"
	OutlineColor="#2196f3">
                    <DatePicker Format="MM/dd/yyyy" Date="{Binding FollowUpDate , Mode=TwoWay}" >
                    </DatePicker>
                </Frame>
            </StackLayout>
            <StackLayout Grid.Row="1">
                <Label  HorizontalOptions="Start" FontSize="Large" VerticalOptions="Center" Text=" - Tools :" ></Label>
                <Frame
    CornerRadius="10"
	Padding="5"
    Margin="5"
	HasShadow="True"
	OutlineColor="#2196f3">
                    <Editor Text="{Binding FollowUpTools , Mode=TwoWay}"  BackgroundColor="Transparent" HeightRequest="80" TextColor="Black"  />
                </Frame>
            </StackLayout>
            <StackLayout Grid.Row="2">
                <Label  HorizontalOptions="Start" FontSize="Large"  VerticalOptions="Center" Text=" - Goals :" ></Label>
                <Frame
   CornerRadius="10"
	Padding="5"
   Margin="5"
	HasShadow="True"
	OutlineColor="#2196f3">
                    <Editor Text="{Binding FollowUpGoals , Mode=TwoWay}" BackgroundColor="Transparent" HeightRequest="80" TextColor="Black" />
                </Frame>
            </StackLayout>
            <StackLayout Grid.Row="3">
                <Label  HorizontalOptions="Start" FontSize="Large"  VerticalOptions="Center" Text=" - Add Notes :" ></Label>
                <Frame
    CornerRadius="10"
	Padding="5"
    Margin="5"
	HasShadow="True"
	OutlineColor="#2196f3">
                    <Editor Text="{Binding FollowUpAddNote , Mode=TwoWay}" BackgroundColor="Transparent" HeightRequest="80" TextColor="Black" />
                </Frame>
            </StackLayout>
            <Button BackgroundColor="#2196f3"
                 Text="Save"
                    WidthRequest="300"
                    CornerRadius="20"
                    FontSize="Medium"
                    TextColor="White"
                Clicked="SaveFollowUpBtn"
                Command="{Binding AddFollowUpCommand}"
                     ></Button>
        </StackLayout>
    </ScrollView>

</ContentPage>